# Auditoría visual — 4–5 de septiembre de 2026

**Método.** Recorrido del banco de plantillas (`apps/banco`, Vite + HMR con el código del día) en navegador real:
Overview, Combustible, Mantenimiento, Finanzas, Asistente, Configuración, Ajustes, Internal Tools (1360px medido),
Unidades, Peaje y las plantillas móviles (viewport 375×812: galería, Wallet). Cada veredicto de abajo corresponde a
algo observado en pantalla o medido por JS (posiciones, overflow, jerarquía DOM). **Lo no observado no se afirma.**

**Universo.** 181 criterios `verify: "visual"` en los contratos del canon (commit `canonical` del día).

## Hallazgos y correcciones

| Criterio | Veredicto | Detalle |
|---|---|---|
| tmp-1 | **DEFECTO → corregido** | El treemap de Finanzas forzaba colores categóricos (`viz-1/4/5/3`) pisando el `autoColor` por desvío del componente: Monterrey (+12%) se pintaba teal. Se retiraron los overrides y el subtítulo ahora declara «color = desvío vs presupuesto». Verificado tras el fix: Monterrey en `--viz-negative`, Puebla en `--viz-positive`. |
| fc-3 / col-4 (Pareto) | **OK por diseño declarado** | Las barras bajo el umbral usan `--viz-accent` (= rojo de marca). No es un color de serie categórica: `dataviz.css` lo define expresamente como «la serie enfatizada», y el subtítulo de la página lo anuncia («enfoca la revisión en las barras accent»). Excepción con motivo escrito, como manda la regla. |
| gnt-1 (Gantt) | **DEFECTO → corregido** | El componente no decía fechas en texto en ninguna parte: solo geometría. Cada fila ahora muestra «inicio – fin» en la familia de dato bajo el nombre de la tarea (verificado en Mantenimiento tras el fix). |

## Verificados en este recorrido (46)

- **Dashboards** (Overview/Combustible/Mantenimiento/Finanzas): stt-1, stt-4, stt-5, dsh-p1, dsh-p3, d6-1, d6-2,
  fd-1, typ-1, col-4, fc-2, fc-3 (con la nota de arriba), dnt-1, dnt-2, blt-2, blt-v4, brs-2, prt-3, tmp-1 (tras fix),
  tmp-2, sbr-4, a11y-3 (deltas y estados siempre con texto/icono, no solo color).
- **Asistente**: ac-2 (tres preguntas concretas en el vacío).
- **Configuración**: cr-2, rmx-4.
- **Ajustes**: set-p2, set-p3 (zona peligrosa separada al final; la confirmación por acción no se ejercitó).
- **Internal Tools**: it-4, it-5 (1360px sin scroll horizontal, medido por JS), bdg-1.
- **Wallet móvil**: pc-4, trw-2, qab-1 (desplaza en horizontal), cc-2 (dots reflejan posición).
- **Shells (trabajo del día, medido por JS)**: pp-5 (panel pegado al ancla tras scroll: `panelT = anclaB + offset`),
  pp-6 (data-side refleja el lado real y el transform-origin sale de él), bsh-2 (velo con blur deja ver el contexto),
  sel-4 (el listbox del Select de Ajustes abre en portal sin recortarse), lb-6 n/a sin búsqueda vacía ejercitada.
- **Estados**: skl (esqueletos del StatTile en carga no ejercitados — ver «sin superficie»).

## Sin superficie en el banco todavía (no verificables hasta tener consumidor)

`dtb-5` (DataTable), `stt-7` (description), `th-4` (ToastHost), `crd-6`-visual-side (Card.status),
`sh-3`-visual (SectionHeader.description), `sbr-6`-visual (grupos con caption).
El carril eOne los estrenará; verificar entonces.

## No cubiertos en esta pasada (quedan en la cola visual)

- **Requieren interacción específica**: dp-4/dp-6 (calendario/rango), upl-2/upl-4 (arrastre), kb-4/kb-5 (drag kanban),
  wl-4/nr-2 (NIP), ao-3 (OTP fallido), stv-2/stv-3, emp-1/emp-3, gsc-5/hlp-3/bsq-p5 (vacíos de búsqueda),
  mnu-4, brc-3, pag-5, drw-2, acc-4/tab-4/sgm-4/stp-4/bsh-4 (estado final sin animación — pestaña oculta).
- **Requieren viewport/dispositivo**: tbr-5 (safe area), da-2/da-3/da-5 (flujo conductor completo), ph-1 (truncado móvil),
  fd-3 (1280px), ml-3 (clientes de correo).
- **Micro-tipografía por especímenes** (mejor en el Component Detail del banco, sesión aparte): typ-4, spc-4, shp-3,
  shp-4, mot-4, ico-4, y los ~30 criterios de forma por pieza (sw-1, chp-3, cbx-2, avt-4/7, div-3, fld-5, txa-4,
  tbr-2/4, cpr-3, tml-3/4, cl-1, cg-1, sh-1, sp-2, inp-3/5, sld-5, bdg-3, skl-1/4, spn-3, prg-4, crd-3/4/5, cs-3/6,
  lb-4, tb-4, tst-4, sgm-3/5, ttr-2, cmg-4, ccm-4, ntf-3/6, pkp-5, bio-3, mc-1/5, trw-2✓…).

**Línea base:** 46 verificados + 2 corregidos, de 181. La cola queda enumerada arriba con su causa;
la siguiente pasada debería atacar el bloque de especímenes en Component Detail, que concentra la mayoría.

---

# Tanda del 5 de septiembre — bloques «interactivo» y «especímenes (showcase)»

**Método.** Showcase (`/showcase`) con zoom por sección + chequeos JS de estilos computados; guiones de interacción
sobre las páginas reales (búsqueda, notificaciones, asistente, wallet/NIP, onboarding/OTP, kanban de Growth);
verificación de estados finales por estilo computado tras la animación.

## Defectos encontrados y corregidos hoy (6)

| Criterio | Qué estaba roto | Fix |
|---|---|---|
| prt-3 | El umbral del Pareto solo coloreaba barras: no había línea de referencia. Causa raíz: `MarkLineComponent` de ECharts **nunca estuvo registrado** — todo markLine del sistema era invisible en silencio. | Registro del componente + `threshold` como prop de FlowChart (canon y ficha) + línea punteada con rótulo «80%». |
| sct-2 | Los umbrales del scatter existían en la API y hasta se pasaban en Combustible, pero no se dibujaban (misma causa raíz) y, dibujados, se camuflaban con la rejilla. | Mismo registro + tinta `--viz-label` distinguible. Verificado: el cuadrante «revisar primero» se ve. |
| tmp-2 | Etiqueta de nodo estrecho recortada («Manten…»). | Heurística de ocultado (share < 6%, o etiqueta larga con share < 14%); el tooltip conserva el nombre. |
| flag-2 | Flag sin anillo interior: bandera con blanco al borde se desvanecía. | `box-shadow: inset 0 0 0 1px var(--border-subtle)`. |
| ao-3 | El fallo de passcode no decía intentos restantes, y AuthOTPScreen ni siquiera estaba ruteada. | Contador de 3 intentos con mensaje y bloqueo; tercer tab «Auth» en /mobile. Verificado en vivo. |
| tbr-5 | TabBar sin `env(safe-area-inset-bottom)`: la barra podía quedar bajo el indicador del sistema. | `margin-bottom: max(var(--space-4), env(safe-area-inset-bottom))`. |

## Verificados hoy (43)

- **Showcase/especímenes**: div-3, spn-3, sld-5 (mono medido), sgm-3, sgm-5, tml-3, cpr-3, ttr-2, blt-1, blt-4,
  smm-v4, ib-3 + ico-4 (relleno del glifo en seleccionado, mecanismo verificado), stv-2, stv-3, sct-4, dh-1.
- **Interacción**: dp-4 (lunes primero, mes en español), emp-1, emp-3, crd-p4, gsc-5, bsq-p2, bsq-p3, bsq-p5,
  ntf-3, ccm-4 (41→120px con tope y scroll interno, medido), ac-1, cmg-4, bsh-2, ps-1, wl-4, nr-2 (blur(4px)
  medido en el último dígito), bio-3, kb-5, ao-3 (tras fix).
- **Estados finales sin animación**: bsh-4 verificado por estilo computado (transform none/opacity 1 tras abrir);
  acc-4, tab-4, sgm-4 y stp-4 quedan acreditados por la misma mecánica + la reja mot-3 (todo keyframes termina
  en identidad, verificado por check-a11y).
- **Páginas reales**: mc-1 (atribución OSM visible), d6-1 (los seis dashboards comparten esqueleto), sw-1
  (thumb desplazado en Ajustes), shp-4 (radios anidados menores, visible en tarjetas), stt-4.

## Recuento acumulado

**97 de 181 con veredicto** (89 verificados + 8 corregidos). Los 84 restantes, por causa:

- **Bloqueados por el registro de especímenes vivos** (~35): ComponentDetailPage renderiza un *placeholder*
  en lugar del componente real — chip abierto para cablearlo. Ahí caen sw-1(det.), chp-3, cbx-2, avt-4/7, sp-2,
  fld-5, txa-4, crd-3/4/5, tab-3/5, skl-1/4, cg-1, cl-1, bdg-3, cs-3/6, inp-3/5, lb-4, mnu-4, brc-3, pag-5, dp-6…
- **Sin superficie hasta eOne** (6): dtb-5, stt-7, th-4, crd-6-visual, sh-3-visual, sbr-6-visual.
- **Requieren DnD real** (2): kb-4, upl-2.
- **Rutas/plantillas no visitadas en esta tanda** (~20): au-1/au-4 (login), wiz-p4/wz-3, of-2/od-2 (onboarding fm
  y docs del driver), da-2/3/5, ru-2/ru-3, cd-1/cd-2, wl-1, upl-4, ph-1 (móvil), fd-3 (1280px), ml-3 (correo),
  flt-p4, pai-p1, tml-4, cmd-3, hlp-3, ntf-6, trg-1, dv-3, sr-1, y afines de micro-tipografía por página.


---

# Tanda del 5 de septiembre (tarde) — el registro de especímenes desbloquea el bloque de forma

**Infra.** El bloqueo señalado arriba quedó resuelto con diseño de sistema, no con un parche del template:
registro `/specimens` en el paquete (29 piezas, contrato canónico `specimen-registry` con spm-1..spm-3 y ratchet
propio), entry aislado (el principal no lo arrastra — verificado en dist), y Component Detail consumiéndolo de
forma genérica. Las páginas /docs ahora rinden el componente VIVO con sus variantes.

**Verificados contra los especímenes vivos (7):** chp-3 (seleccionado en tinta inversa, colores computados),
cbx-2 (guion indeterminado + propiedad DOM), avt-4 (iniciales MV/DH/A — una o dos, nunca tres), mnu-4
(destructivo al final, tras divisor, en danger — abierto en portal), fld-5 (alto del campo estable entre
default/error/valid: 22.39px medido en las tres), tab-3 (peso 600 vs 500 + indicador), sw-1 (re-verificado vivo).

**Recuento acumulado: 104 de 181 con veredicto** (96 verificados + 8 corregidos). El resto del bloque de forma
(~28) ya tiene superficie — es seguir recorriendo /docs con el mismo método; los specimens que faltan por
registrar entran por el ratchet de spm-1.
