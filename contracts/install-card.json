{
  "id": "install-card",
  "name": "InstallCard",
  "layer": "patterns",
  "function": [
    "display"
  ],
  "status": "stable",
  "summary": "Card inverse con nombre de plataforma, badge de status y CodeBlock con comando de instalacion.",
  "conformance": {
    "criteria": [
      {
        "id": "ic2-1",
        "rule": "El comando es copiable via CodeBlock.",
        "level": "must",
        "verify": "manual"
      },
      {
        "id": "ic2-2",
        "rule": "El comando viaja por CodeBlock copyable: el boton de copiar existe con nombre accesible.",
        "level": "must",
        "verify": "automated"
      }
    ]
  },
  "implements": [
    "card",
    "code-block",
    "badge"
  ],
  "nonGoals": [
    "No ejecuta el comando: solo lo muestra."
  ],
  "platforms": {
    "web": "adopted"
  },
  "reference": {
    "src": "patterns/InstallCard.tsx"
  },
  "api": {
    "members": [
      {
        "name": "platform",
        "kind": "prop",
        "type": "string",
        "required": true
      },
      {
        "name": "command",
        "kind": "prop",
        "type": "string",
        "required": true
      },
      {
        "name": "status",
        "kind": "prop",
        "type": "string"
      },
      {
        "name": "statusTone",
        "kind": "prop",
        "type": "BadgeTone"
      }
    ]
  }
}
