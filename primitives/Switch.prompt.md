Switch para estados on/off inmediatos (no requiere guardar). El pulgar se estira al presionar — golosina firmada de Flow.

```jsx
<Switch checked={on} onChange={setOn} label="Notificaciones push" />
```
