Radio con punto que entra por resorte; `description` para opciones explicadas (wizards).

```jsx
<Radio name="plan" value="pro" checked={p==='pro'} onChange={setP} label="Pro" description="Flotas de 10+ unidades" />
```
