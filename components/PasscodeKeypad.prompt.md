Teclado numerico con dots animados para passcode. Tecla biometrica opcional.

```jsx
<PasscodeKeypad value={pin} onChange={setPin} onComplete={check} biometricIcon="fingerprint" onBiometric={bio} />
```
