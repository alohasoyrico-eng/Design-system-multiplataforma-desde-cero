Lista de mensajes con autoscroll. `content` en cada mensaje permite respuestas ricas embebidas.

```jsx
<ChatThread messages={messages} emptyState={<EmptyState icon="forum" title="Pregunta lo que quieras" />} />
```
