Comparativa por categoria sobre `FlowChart` (`type="bar"`); la barra maxima va en accent con `highlightMax`.

```jsx
<Bars data={[{label:"Lun",value:42},{label:"Mar",value:58}]} format={(v)=>"$"+v} />
```
