Estado vacío: qué no hay + qué hacer.

```jsx
<EmptyState icon="local_taxi" title="Sin unidades activas"
  description="Cuando una unidad se conecte aparecerá aquí."
  action={<Button variant="accent" icon="add">Agregar unidad</Button>} />
```
