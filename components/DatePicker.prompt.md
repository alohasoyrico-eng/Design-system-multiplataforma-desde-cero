Campo de fecha con calendario flotante (lunes primero, hoy con anillo, días con micro-zoom al hover).

```jsx
<DatePicker value={date} onChange={setDate} min="2026-01-01" />

<DatePicker mode="range" value={range} onChange={setRange} />
```
